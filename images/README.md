# Images folder

Place your images and icons here, for example:

- `logo.png` — site logo
- `hero-bg.jpg` — hero section background (optional; the site uses a placeholder URL)
- `favicon.ico` — browser favicon

The site uses inline SVGs for icons and a placeholder image URL for the hero background so it works without local images.
